# Minimal Supabase CLI config for local and remote migrations.
# Link a hosted project with `npx supabase link` before applying remotely.

project_id = "stocksignal"

[api]
enabled = true

[db]
major_version = 17

[db.seed]
enabled = false
